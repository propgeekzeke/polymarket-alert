services:
  - type: web
    name: polymarket-alert
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT --workers 1 --threads 2
    envVars:
      - key: DISCORD_WEBHOOK
        sync: false
